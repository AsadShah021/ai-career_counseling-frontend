version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # this must match the “Build output directory” field in the console
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
